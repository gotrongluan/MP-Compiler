
            function abc():real;
            begin
                a := a + 1;
                return 2.5;
            end
            function xyz():real;
            begin
                a := a - 2;
                return 3.2;
            end
            function mnp():integer;
            begin
                return a + 2;
            end
            var a:integer;
            procedure main();
            var t:real;
            begin
                a := 10;
                t := abc() - xyz() + mnp() / abc();
                putFloat(t);
                t := a mod mnp() * abc() - mnp();
                putFloat(t);
                t := mnp() - mnp() / abc() - xyz() + abc() / xyz() + a;
                putFloat(t);
            end   
            