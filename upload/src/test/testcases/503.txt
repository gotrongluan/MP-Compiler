
            var a: array[-1 .. 2] of integer;
                b: array[-1 .. 2] of real;
                c: array[0 .. 5] of boolean;
                d: array[-10 .. -2] of string;
            procedure MAIN();
            var i: integer;
            begin
                for i := 2 downto -1 do
                    putIntLn(a[i]);
                for i := 2 downto -1 do
                    putFloatLn(b[i]);
                for i := 0 to 2 do
                    putBoolLN(c[i]); 
                for i := -4 to -3 do
                    putStringLn(d[i]);
                for i := 2 to -1 do
                    putIntLn(a[i] - i);
                for i := 5 to 0 do
                    putStringLn("trong luan");
            end
            