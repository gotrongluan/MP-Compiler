
            function x():boolean;
            begin
                a := a + 1;
                return true;
            end
            function nx():boolean;
            begin
                a := a + 1;
                return false;
            end
            var a:integer;
            procedure main();
            begin
                a := 20;
                putInt(a);
                putBool(x() or else nx());
                putInt(a);
                putBool(nx() or else x());
                putInt(a);
            end
            