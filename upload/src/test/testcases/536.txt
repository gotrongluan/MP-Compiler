
            procedure foo(x:array[0 .. 5] of integer);
            begin
                with i:integer; do
                    for i := 0 to 5 do
                    begin
                        x[i] := x[i] + 1;
                        putInt(x[i]);
                    end
            end

            procedure main();
            var y: array[0 .. 5] of integer;
            begin
                y[0] := 2;
                y[1] := 12;
                y[2] := 1998;
                y[3] := 16;
                y[4] := 2000;
                y[5] := 11;
                with i:integer; do
                    for i:= 0 to 5 do
                        putInt(y[i]);
                foo(y);
                with i:integer; do
                    for i:= 0 to 5 do
                        putInt(y[i]);
            end
            