
            function isPositive(n:integer):boolean;
            begin
                if n > 0 then
                    return TRUE;
                else
                    return FALSE;
            end

            var m: integer;
            procedure main();
            var n: integer;
            begin
                m := 2;
                n := -1000;
                putBoolLn(isPositive(m));
                putBoolLn(isPositive(n));
            end
            