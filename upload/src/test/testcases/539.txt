
            procedure foo(x:array[0 .. 2] of boolean);
            var i:integer;
            begin  
                for i := 0 to 2 do
                    x[i] := not x[i];
            end
            var x:array[0 .. 2] of boolean;
            procedure main();
            var i:integer;
            begin
                for i := 0 to 2 do
                    x[i] := i > 1;
                for i := 0 to 2 do
                    putBool(x[i]);
                putLn();
                foo(x);
                for i:= 0 to 2 do
                    putBool(x[i]);
            end
            